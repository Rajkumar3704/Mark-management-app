<%= form_with(model: course_student) do |form| %>

    <% if course_student.errors.any? %>
          <div style="color: red">
            <h2><%= pluralize(course_student.errors.count, "error") %> prohibited this course_student from being saved:</h2>
            <ul>
              <% course_student.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
    <% end %>

    <div>
      <%= form.label :c_id, style: "display: block" %>
      <%= form.number_field :c_id %>
    </div>

    <div>
      <%= form.label :s_id, style: "display: block" %>
      <%= form.number_field :s_id %>
    </div>

    <div>
      <%= form.submit %>
    </div>

<% end %>
